# 프로젝트 규약 (CLAUDE.md)

이 파일은 세션이 바뀌어도 유지되는 이 프로젝트의 고유 설정·진행 규약이다. 규약이 바뀌면 즉시 갱신한다.

## 주제
- **성장형 학습 프로젝트**: Spring Boot로 SBB(질문/답변 게시판)를 단계별로 구현.
- 사용자가 학습 주체, Claude는 강사/보조. **모든 진행은 한국어.**

## 학습 자료
- 주 자료: https://www.slog.gg/p/14163
- 참조 코드: https://github.com/jhs512/p-14163-1 (main, 커밋 002~039 = 단계 매핑). 이전 시즌: jhs512/p-13959-1.
- 강↔커밋 대응을 따르되 난이도가 매끄럽게 오르도록 조정.

## 스택
- Spring Boot 4.0.6 · Java 25(Temurin) · Gradle 9.4.1(Kotlin DSL) · Lombok · Thymeleaf · JPA/H2
- 패키지: `com.mysite.sbb`

## 학습 진행 방식 — 매 사이클 3단계 (순서 고정, 시각화 주도)
1. **구체적 시각화**: 단원 구조·흐름·관계를 다이어그램으로 먼저 제시(인라인 렌더링).
2. **코드 매핑 설명**: 그림 상자 → 실제 코드/실행값 대응으로 한 흐름에 빠르게 설명.
3. **자동 반영 + 커밋**: Claude가 코드를 직접 작성→빌드/실행 검증→git 커밋+push 자동. 사용자는 타이핑 안 함.

## 핵심 원칙
- 한 사이클 = 개념/기능 하나. 통과 조건(빌드/실행/테스트 성공)을 건다.
- 작은 수직 슬라이스. 한 번에 다 주지 않는다. 이미 아는 건 반복 설명 금지.
- 깨진 코드 금지: 커밋 전 빌드/실행으로 검증. 검증용 임시 코드는 원복.
- 실제 실행값(로그·쿼리·변수·분기)으로 확인.

## 검증법
- 빌드/테스트: `./gradlew build` (또는 `./gradlew test`)
- 실행: `./gradlew bootRun` (기본 포트 8080)

## 기록 (note/)
- 단원마다 `note/NN-제목.md`: 머릿속 그림 요약 · 핵심 내용 · 디버깅 정리(실제 값) · 완료 상태.
- 전체 진도표: `note/README.md`.

## Git 자동화
- 사용자는 git을 신경 쓰지 않는다. 단원이 검증을 통과하면 Claude가 커밋+push.
- 원격: https://github.com/jomin4/prgrms-Springboot_jsb (public, origin, main).
- 커밋 메시지: 의미 있는 한국어, 형식 `N단원: 제목` (묶음은 `A~B단원: 제목`).
